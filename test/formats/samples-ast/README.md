# MSON AST samples

These MSON AST samples are generated from MSON samples in `test/formats/samples-mson` directory.
If you change MSON files, add new or remove some, you need to re-run `scripts/generate-samples-ast.coffee`
script to have these ASTs up-to-date.
